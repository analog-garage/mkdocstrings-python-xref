# dependencies need for testing
channels:
  - garage-conda-local
  - conda-forge

dependencies:
  # build
  - hatch-garconda >=0.2.3
  # test
  - coverage >=7.3.0
  - pytest >=7.4.0
  - pytest-cov >=4.1.0
  - pylint >=2.17.5
  - mypy >=1.5.1
  - mkdocs-material >=8.2
  - beautifulsoup4 >=4.12
  # Fix or remove conda #40
#  - tox >=3.25
#  - tox-conda >=0.9.2
